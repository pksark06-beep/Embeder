{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability: the app window may invoke Embeder's custom commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
